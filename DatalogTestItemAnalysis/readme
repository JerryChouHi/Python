功能介绍：
	将Datalog数据根据测试项的数据分布各自生成(png)图片以供分析。

操作步骤：
	1.点击Open，选择csv文件
	2.输入数据：
  	Sigma(采用数据的范围，默认值：3)
  	DPI(图像每英寸长度内的像素点数，默认值：300)
	3.点击Run

生成结果：
	在csv文件同级目录生成一个‘同名_sigmaX_dpiY’的文件夹，存放test item同名的png图片（windows不支持’/’，替换为’_’）。
